
#include <cstdio>
#include <gtest/gtest.h>
#include <stdint.h>
#include <stdlib.h>

#include "depktcap.h"

TEST(parser, captureEther) {
    EXPECT_EQ(1+1, 2);
}

TEST(parser, captureIP) {
    EXPECT_EQ(1+1, 2);

}